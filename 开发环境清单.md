# 开发环境与工具存储位置清单

> 创建时间：2026-07-26
> 环境：WSL2 + Ubuntu 22.04

---

## 一、WSL 虚拟机

| 项目 | 详情 |
|------|------|
| **发行版** | Ubuntu-22.04 |
| **WSL版本** | 2 |
| **虚拟磁盘位置** | `D:\wsl\ubuntu2204\ext4.vhdx`（26.3 GB） |
| **进入方式** | 终端输入 `wsl` 或 `bash` |
| **Linux用户** | kaltsit |
| **Linux HOME** | `/home/kaltsit` |

---

## 二、AFL++（模糊测试工具）

| 项目 | 详情 |
|------|------|
| **安装方式** | `sudo apt install -y afl++` |
| **版本** | 4.00c |
| **编译器** | `afl-cc`（实际路径：`/usr/bin/afl-cc`） |
| **主程序** | `afl-fuzz`（实际路径：`/usr/bin/afl-fuzz`） |
| **文档路径** | `/usr/share/doc/afl++-doc/` |
| **实验目录** | `~/fuzz-lab/01-hello-fuzz/` |

---

## 三、Clang / LLVM（编译器 + 内建Fuzzer）

| 项目 | 详情 |
|------|------|
| **安装方式** | 随 AFL++ 自动安装（clang-14） |
| **clang路径** | `/usr/bin/clang` → clang-14 |
| **clang版本** | 14.0.0 |
| **libfuzzer路径** | `/usr/lib/llvm-14/lib/clang/14.0.0/lib/linux/libclang_rt.fuzzer-x86_64.a` |
| **AddressSanitizer** | 随 clang 内建（编译时加 `-fsanitize=address`） |

---

## 四、ROS 2 Humble（机器人操作系统）

| 项目 | 详情 |
|------|------|
| **安装方式** | `sudo apt install -y ros-humble-desktop` |
| **安装路径** | `/opt/ros/humble/` |
| **核心子目录** | |
| | `setup.bash` → 环境变量脚本 |
| | `bin/` → 可执行文件（ros2, talker, listener等） |
| | `lib/` → 共享库（rclcpp, rclpy, DDS等） |
| | `include/` → 头文件 |
| | `share/` → 消息定义、配置文件 |
| **快速体验** | `ros2 run demo_nodes_cpp talker` |
| | `ros2 run demo_nodes_cpp listener` |
| **bashrc配置** | `echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc` |

---

## 五、ROS 2 关键子包（你以后要审计的）

| 包名 | 路径 | 功能 | 攻击面 |
|------|------|------|--------|
| **rmw_fastrtps_cpp** | `/opt/ros/humble/lib/librmw_fastrtps_cpp.so` | Fast-DDS中间件 | RTPS协议解析 |
| **rosbag2_cpp** | `/opt/ros/humble/lib/librosbag2_cpp.so` | 数据录制回放 | 文件反序列化 |
| **rclcpp** | `/opt/ros/humble/lib/librclcpp.so` | ROS 2 C++客户端库 | 节点通信 |
| **sros2** | `/opt/ros/humble/share/sros2/` | 安全扩展 | 证书验证 |
| **tf2** | `/opt/ros/humble/lib/libtf2.so` | 坐标变换 | 变换数据解析 |

> ⚠️ 以上是 APT 安装的**二进制版本**（无源码）。要做代码审计和 fuzz 需要自己编译源码版本，第3-4周再做。

---

## 六、系统基础工具（Ubuntu 22.04 默认）

| 工具 | 路径 | 版本 |
|------|------|------|
| **gcc** | `/usr/bin/gcc` | 11.4.0 |
| **g++** | `/usr/bin/g++` | 11.4.0 |
| **python3** | `/usr/bin/python3` | 3.10.x |
| **cmake** | `/usr/bin/cmake` | 随 ros-dev-tools 安装 |
| **git** | `/usr/bin/git` | 系统预装 |
| **gdb** | `/usr/bin/gdb` | 系统中预装（需 `sudo apt install gdb`） |

---

## 七、实验文件

| 文件 | 路径 | 用途 |
|------|------|------|
| vuln.c | `~/fuzz-lab/01-hello-fuzz/vuln.c` | 第一个有bug的示例程序 |
| vuln_fuzz.c | `~/fuzz-lab/01-hello-fuzz/vuln_fuzz.c` | libfuzzer适配版 |
| crash.c | `~/fuzz-lab/01-hello-fuzz/crash.c` | memset溢出验证程序 |
| seeds/ | `~/fuzz-lab/01-hello-fuzz/seeds/` | fuzz种子输入 |
| fuzz_output/ | `~/fuzz-lab/01-hello-fuzz/fuzz_output/` | fuzz输出（crash等） |

---

## 八、如何启动工作环境

```bash
# 1. 打开终端 → 进入 WSL
wsl

# 2. 确认 ROS 2 环境已加载
ros2 --version   # 应输出版本号

# 3. 进入实验目录
cd ~/fuzz-lab/01-hello-fuzz/

# 4. 开始工作
```

---

## 九、空间使用

| 磁盘 | 总容量 | 已用 | 空闲 | 用途 |
|------|--------|------|------|------|
| WSL ext4.vhdx | ~27 GB | ~27 GB | 少量 | Linux系统 + ROS 2 + 工具 |
| D盘 | 未知 | 未知 | ~93 GB | vhdx所在物理盘 |
