# 2026-08-04 学习笔记

## 核心收获
1. **主线 fuzz 进入"自愈式"持续运行**——搭好了自动重启循环，这周可以丢着不管
2. 排查并修好了 **4 个让 fuzz 停掉的坑**（今天最有价值的动手经验）
3. **辅线读完 CHAI 论文**，理解了"视觉提示注入 vs FGSM"，纠正了 3 个思考题

---

## 一、Fuzz 今天修好的 4 个坑

| # | 现象 | 原因 | 修复 |
|---|------|------|------|
| 1 | WSL 里找不到项目路径 | 路径少写了 `study/` 这一层 | 正确路径是 `/mnt/c/Users/asus/Desktop/study/科研相关/...` |
| 2 | fuzz 跑完立即退出 | `-runs=0` = "只处理语料库"≠"无限" | 无限用 `-runs=-1` |
| 3 | 空输出 + 段错误 | **ASAN shadow 初始化失败**：PIE 程序随机加载撞上 shadow 内存区（WSL2 已知坑，与真实漏洞无关） | `setarch -R` 禁用 ASLR，3 次启动 0 报错 |
| 4 | OOM 退出（exit 71） | 语料库爆炸（14 分钟 2445→17464 个），RSS 随 corpus 涨，撞默认 2GB 上限 | `-rss_limit_mb=4096` + 定期 `-merge=1` 最小化 |

**关键认知**：坑 3 里"段错误"容易让人误以为是 fuzz 找到了 crash——**不是**，那是 ASAN 自己的启动故障。以后判断"是否真 crash"要看有没有 ASAN 报告和 `crash-` 文件，而不是只看退出码。

### 自动重启循环（本周核心交付）

```
run_fuzz_loop.sh 每周期：
  ① ./fuzz_onnx corpus_new/ corpus/ -merge=1     # 合并去冗余（17464→3085个）
  ② setarch -R ./fuzz_onnx corpus_new/ -runs=-1 ...  # 无限跑，直到 OOM/崩溃/timeout
  ③ 回收语料 → 回到 ①
```

- 每天看两个地方：`~/fuzz-lab/fuzz_loop.log` 尾部 + `~/fuzz-lab/` 有没有 `crash-*` 文件
- 启动命令：`wsl bash -c "cd ~/fuzz-lab && bash run_fuzz_loop.sh"`

### 今天结束时 fuzz 状态
```
exec/s: ~32-40k     cov: 2574     ft: 15495     corpus: ~3180 个（去重后）
crash: 1 个（见下方第三节）   OOM 已由循环自动处理
```

---

## 二、CHAI 论文：视觉提示注入 vs FGSM

CHAI = Command Hijacking against embodied AI（对具身AI的命令劫持），arXiv:2510.00181

**一句话**：攻击者不黑软件，只在机器人摄像头视野里放一块写文字的牌子，LVLM 看到后输出恶意指令文本，下游控制器忠实执行——无人机在敌方区域降落、汽车冲进人行横道。

**最该记住的概念**：
- 攻击"命令层"（LVLM 输出的中间文本），不是感知层（识别结果）
- **FGSM 骗眼睛（看错），CHAI 骗"大脑"（说错）**
- 双重优化：同时调"牌子写什么"（语义）和"牌子长什么样"（颜色/字体/位置）
- 黑盒：交叉熵法（CE），免梯度

**关键数字**：物理小车 ≥87%、CloudTrack 95.5%、DriveLM 81.8%、比 SceneTAP 强 10 倍、跨中英西三语、恶劣天气有效。

---

## 三、⭐ 三个思考题：我的回答 + 纠错

### 第 1 题：通用对抗标牌 vs 通用对抗扰动，是同一回事吗？

**我的回答**：并不是。CHAI 攻命令层、黑盒用交叉熵；FGSM 改输入图像加不可见噪声、白盒用梯度。

**纠错**：**结论反了——思想上就是同一回事**，区别在实现手段。

| 维度 | FGSM 通用扰动 | CHAI 通用标牌 |
|------|--------------|--------------|
| **通用性的思想** | 一张噪声图，贴任意图片都有效 | 一块牌子，放任意场景都有效 |
| 扰动空间 | 连续像素噪声 | 离散语义文字 |
| 优化方法 | 白盒梯度 | 黑盒交叉熵 |
| 攻击目标 | 感知层（读错） | 命令层（说错） |
| 可见性 | 不可见 | 人类可读 |

- 记法：**"通用"= 一个扰动处处泛化**，这是两者的共同灵魂；差别在"扰动的载体和怎么优化"。
- 补充纠错：CHAI 不是"篡改命令"，而是**诱导模型自己输出恶意命令**——牌子是提示（prompt），恶意文本是模型生成的。
- （"FGSN" 是 FGSM 的笔误）

### 第 2 题：恶意 .onnx 模型文件能不能像牌子一样劫持机器人？

**我的回答**：应该不行吧。onnx 是底层代码层，靠内存泄漏等 fuzz 漏洞劫持；牌子是通过命令层劫持，两者不一样。

**纠错**：**判断反了——能，而且比牌子更危险，这正是你主线 fuzz 的意义。**

- 恶意 .onnx 文件只要在 ONNX Runtime **解析时触发内存漏洞**（堆溢出/越界读），就能拿到**任意代码执行（RCE）**，直接控制整个机器人系统——比牌子劫持"决策文本"底层得多。
- 两条线的**信任边界**（题目括号里的提示）：
  - 牌子的信任边界：LVLM **把场景文字当合法输入信任** → 命令层被劫持（只影响决策）
  - .onnx 的信任边界：解析器**把模型文件这种"数据格式"当可信内容直接反序列化、不充分校验** → 数据变成代码执行（影响整个系统）
- **我在回答里把方向搞混了**：fuzz 出的内存漏洞不是用来"劫持 LVLM 模型本身"，而是用来劫持**承载它的软件（ONNX Runtime）**。牌子劫持的是"模型的行为"，fuzz 劫持的是"宿主软件的控制权"。一个是打模型语义，一个是打软件内存。

> 一句话：**牌子劫持机器人的"想法"，恶意 .onnx 劫持机器人的"身体"。**

### 第 3 题：CHAI 为什么不能用 FGSM 的梯度法？

**我的回答**：命令层攻击难以定位出错点，因为是模型内部计算、黑盒，只能用交叉熵等统计量；FGSM 是白盒能看清哪里出错。

**纠错**：主因对了（黑盒拿不到梯度），但表述要精炼，且漏了一条：

1. **主因**：FGSM 要算 ∇_x Loss，依赖**白盒反向传播**（模型权重、中间梯度全可见）；CHAI 只能黑盒查 API，梯度根本算不出来 → 只能用免梯度优化器（交叉熵法）。
2. **补充（易漏）**：CHAI 的攻击参数有一部分是**离散的**（选哪个词？什么字体？放什么位置？）——梯度在离散空间上**没有定义**。就算模型是白盒，FGSM 这种"沿梯度微调连续量"的思路也套不上去。
3. 一句话：**白盒=能算梯度=FGSM 可行；黑盒=算不了梯度=只能采样统计（CE）。**

---

## 三.5、🚨 第一个 crash（当日重大事件）

下午循环跑到第 **23,063,272 次**执行时，抓到第一个 ASAN 崩溃。

- **文件**：`crash-06f562aae3f37f5aa7ecc84d8364b9f7cd5bd6fe`（10501 字节，已备份到项目 `crash_analysis/`）
- **类型**：SEGV（READ 零页地址 0xa = NULL+10），`operator delete` 时崩溃
- **栈**：`ModelProto::~ModelProto` → FunctionProto → NodeProto → AttributeProto → GraphProto → `RepeatedPtrField::Destroy` 释放非法指针
- **本质**：**堆损坏**——真正的破坏来自更早的某个输入，这个输入只是"析构时踩雷的受害者"
- **触发变异**：`MS: 3 CopyPart-ShuffleBytes-CopyPart`，父语料单元 `a444090f...`
- **复现**：❌ 单独跑、短变异都不崩（**强状态依赖**，需要 fuzzer 进程里数百万次历史的堆状态）

### 诚实判断（三条）
1. 崩在 **protobuf 3.12.4**（系统老版本库）里，不是 ONNX Runtime 自己的代码——ONNX Runtime 打包的是**新版 protobuf**，此 bug 大概率**不传导**到它
2. 状态依赖 + 稀有（1800 万次才中一次）→ 离"能报 CVE 的报告"还远
3. ✅ 但**证明了 fuzz 流水线真实有效**——真的从堆里抓出了损坏

**关键教训**：fuzz 找到"崩溃" ≠ 找到"ONNX Runtime 漏洞"。这个 crash 是**库 bug**，不是目标引擎的 bug。想挖 ONNX Runtime 自己的 CVE，最终要 fuzz 它的 C++ 实现（graph.cc/model.cc），那是下一步。

---

## 四、今日数字

```
fuzz:    第一个 crash 出现（23M 次触发）→ protobuf 3.12.4 堆损坏，状态依赖
         自动循环持续跑（PID 57, ~40k exec/s），OOM 自动处理
CHAI:    物理小车 ≥87%, CloudTrack 95.5%, DriveLM 81.8%
思考题:  3 题纠错完成（第2题最重要：恶意 .onnx 能劫持机器人且更危险）
```

---

## 策略决定（和导师对齐）

和导师讨论了成果路径。结论：**不卷 VLM 攻击**（拥挤+需GPU），聚焦 advisor 的"软件基础设施漏洞挖掘"方向。三条线并行：

```
① 画攻击面（新开）→ 攻击面分析-ONNX模型加载链.md（已完成初稿）
② 继续 fuzz（持续）→ 自动循环在跑，43M 次/1 crash
③ 问导师下一步方向 → 问题清单见下
```

**攻击面文档核心**：恶意 .onnx 文件 → [1]反序列化 → [2]图构建 → [3]内存规划 → [4]算子 → [5]硬件 → 具身AI。今天的 crash 在 [1]。历史 CVE 高发区预计在 [3] shape 整数溢出 和 [4] 算子越界——fuzz 下一步打这两处。

## 问导师的问题清单（下次见面/消息用）

1. **方向取舍**：您更推荐我们先"fuzz 真实 onnxruntime 图构建/算子层"，还是先做"ONNX 生态 CVE 系统性分析"打基础？（我们倾向先分析后 fuzz，用历史 CVE 指导目标）
2. **具体目标**：具身 AI 里优先挖哪条软件栈？（ROS2+ONNX 集成？某个机器人平台的模型加载器？）
3. **编译取舍**：fuzz onnxruntime 本体需要完整编译（比较重），和直接 fuzz onnx.proto 层相比，您觉得怎么取舍？
4. **可参与工作**：您手头有没有可以让我们加入的已开展工作/数据集？

## 明天计划

- [ ] 看 fuzz_loop.log，确认一夜后 cov/corpus/crash 变化
- [ ] 填攻击面文档第四节"CVE 映射表"（从 NVD 拉 ORT 历史 CVE）
- [ ] 把问导师的问题发出去
- [ ] 读 CHAI 论文第九节"防御"
