# 攻击面分析：ONNX 模型加载链 → 具身 AI

> 用途：这是"画攻击面"的主文档，配合 ONNX fuzz 主线使用。
> 目标：把"一个恶意 .onnx 模型文件"从进入系统到最终执行的每一环都标出来，
> 每个环能出什么漏洞、已被哪些 CVE 打过，从而指导 fuzz 打哪里。

---

## 一、攻击链总览（一句话 + 图）

**攻击者只需在 HuggingFace/OTA 更新里投放一个恶意 .onnx 模型文件，机器人一加载就触发引擎漏洞 → RCE → 物理危险。**

```
        攻击者入口
        ┌────────────────────────────────────────────┐
        ▼                                            │
恶意 .onnx 模型文件                                ↑ 恶意内容可来自：
   │                                               │   · HuggingFace 模型库
   ▼                                               │   · 训练平台投毒
[1] 反序列化  ParseFromString (protobuf)  ⭐ 今天崩在这
   │                                               │
   ▼                                               │
[2] 图构建/校验  graph.cc / model.cc (ORT 本体)
   │                                               │
   ▼                                               │
[3] 内存规划     allocator (shape → 分配)
   │                                               │
   ▼                                               │
[4] 算子执行     kernels / XNNPACK / MLAS
   │                                               │
   ▼                                               │
[5] 硬件执行     CPU/GPU/NPU ──────────────────────┘
   │
   ▼
具身AI：OpenVLA / YOLO 感知 / CLIP 视觉语言 → 机器人行为
```

**核心洞察**：模型文件本质是"**数据**"，但加载链把它当"**受信任的代码/内容**"来解析——这条信任边界就是所有漏洞的根。

---

## 二、每一环的漏洞类型（fuzz 目标清单）

| 环节 | 涉及代码 | 漏洞类型 | 触发方式 |
|------|---------|---------|---------|
| [1] 反序列化 | protobuf + onnx.pb.cc | 堆溢出、UAF、double-free、**深度炸弹**（递归过深）、billion laughs（嵌套展开爆炸） | 畸形 protobuf 结构 |
| [2] 图构建/校验 | graph.cc、model.cc | 逻辑 bug、资源耗尽（超大图=DoS）、类型混淆、空指针 | 图结构异常（海量节点/环/属性） |
| [3] 内存规划 | allocator、shape 推导 | **整数溢出→分配过小→堆溢出**、负数尺寸、OOM | 恶意 shape（超大/负数维度） |
| [4] 算子执行 | kernels 实现 | 越界读/写（shape 不匹配）、除零、空张量、数据竞争 | 恶意 shape + 数据 |
| [5] 依赖库 | protobuf、XNNPACK、ONNX-MLIR | 传递性漏洞 | 库本身 bug |

**⭐ 你今天的 crash 落在 [1]**：protobuf 3.12.4 在 RepeatedPtrField 析构时释放非法指针（堆损坏，状态依赖）。

---

## 三、信任边界分析（论文里要写的）

```
信任边界 1：模型文件（数据）→ 解析器（代码）
    解析器默认"文件是合法输入"，不充分校验 → 你 crash 的根源
    类比：浏览器解析 HTML 也是信任"输入是文档"→ 历史上一堆解析漏洞

信任边界 2：模型内容 → 图执行
    模型的 shape/算子会被当成"指令"执行 → 恶意 shape 打 [3][4]

信任边界 3：模型 → 具身AI决策
    模型输出直接驱动机器人动作 → 感知错误(对抗) + 引擎劫持(RCE) 都会传导到物理世界
```

---

## 四、历史 CVE 映射表（第一批已填：onnx 包）

> 数据来源：NVD API + OSV（2026-08-04 实测拉取）。先填 **onnx 包**（参考实现，含 onnx.proto 解析——**正是当前 harness fuzz 的层**）。

### 4.1 onnx 包 CVE（我们的 fuzz 层）

| CVE 编号 | 环节 | 根因 | 影响 | 时间 | 备注 |
|---------|------|------|------|------|------|
| CVE-2022-25882 | [1]反序列化 | 目录遍历 | 信息泄露 | 2023-01 | onnx<1.13.0 |
| CVE-2024-27318 | [1]反序列化 | 目录遍历 | 信息泄露 | 2024-02 | <1.15.0 |
| CVE-2024-27319 | [1]反序列化 | 越界读 OOB | DoS | 2024-02 | <1.15.0 |
| CVE-2024-5187 | [1]文件 | 任意文件覆盖 download_model_with_test_data | 文件破坏 | 2024-06 | 供应链 |
| CVE-2024-7776 | [1]文件 | 路径遍历 download_model | 文件访问 | 2025-03 | 供应链 |
| CVE-2025-51480 | [1]文件 | 路径遍历 save_external_data | 文件访问 | 2025-07 | external data |
| CVE-2026-28500 | [0]供应链 | 不可信仓库警告被 silent=True 抑制 | 恶意模型混入 | 2026-03 | hub 加载 |
| CVE-2026-27489 | [1]反序列化 | Symlink 路径遍历 | 信息泄露 | 2026-03 | |
| CVE-2026-34445 | [1]反序列化 | 恶意模型 crash 服务器（对象解引用） | DoS | 2026-04 | |
| CVE-2026-34446 | [1]文件 | 任意文件读 ExternalData Hardlink Bypass | 信息泄露 | 2026-04 | |
| CVE-2026-34447 | [1]文件 | External Data Symlink 遍历 | 信息泄露 | 2026-04 | |
| CVE-2026-14647 | [2]图构建 | convPool 处理缺陷 | DoS | 2026-07 | onnx≤1.21.x |
| CVE-2026-44512 | [2]转换 | **Upsample Version Converter 空指针** | DoS | 2026-07 | ⭐版本转换器 |
| CVE-2026-63632 | [2]转换 | **Gemm Version Converter 堆越界读** | DoS/泄露 | 2026-07 | ⭐版本转换器 |

### ⭐ 关键洞察（直接指导 fuzz）

1. **2026 年的新 bug 集中在 "Version Converter Adapter"**（Gemm 堆越界读、Upsample 空指针）——这是 onnx 把旧版本模型转换到新格式的代码。**我们的 harness 目前没覆盖**（只做了 `ParseFromString`，没跑版本转换）。
   → **fuzz 扩展方向**：对 `onnx/version_converter/` 写 harness。
2. **external_data 文件访问是高频漏洞区**（路径遍历×4、symlink、hardlink）——加载时读外部数据文件的代码满是路径信任问题。
3. **onnx 包 CVE 高发环节 = [1]反序列化 + 文件访问**，与攻击面图吻合。相对薄弱的 [3][4]（shape/算子）在 onnx 包里出现少——那些主要在 onnxruntime 本体。

### 4.2 onnxruntime 本体：不发表 CVE（重大发现 2026-08-04）

> **关键事实**：ORT 的安全漏洞走 **MSRC 私密追踪**（VULN-#### 内部号），**不公开发 CVE**。
> 证据：issue #23258 的 1.19.2 崩溃 → VULN-143188 → 1.20.1 修复，无 CVE。
> GitHub 安全公告页为空（用户实测确认）。
>
> **战略含义**（推翻"fuzz ORT → 拿 CVE"的旧假设）：
> - ORT 撞出的 bug：报 MSRC → 拿 VULN-#### + 披露致谢 → **没有 CVE 编号**
> - 成果形式改为：论文记录"发现 ORT N 个漏洞，已披露并修复"
> - 想拿 **CVE 编号**，目标应是 **onnx 包自己的代码**（version_converter/checker/external_data——2026 年的 Gemm/Upsample 堆 bug 证明那里有 CVE 流程）

**拉取 CVE 失败的原因**：不单纯是 API 受限——**ORT 本来就不分配 CVE**，所以 NVD/OSV 里搜不到是正常现象，不是数据缺失。

**原始数据脚本**：`fetch_ort_cves.py`（项目根目录，onnx 包部分可复用重跑）。

---

## 五、映射到具身 AI（论文威胁模型）

| 具身AI组件 | 用的模型/库 | 加载链环节 | 若被攻破 |
|-----------|------------|-----------|---------|
| VLA 决策（OpenVLA） | Transformer → .onnx | 全链 | 决策被劫持 |
| 视觉感知（YOLOv8） | .onnx | 全链 | 识别错/引擎崩 |
| 视觉语言（CLIP） | .onnx | 全链 | 感知错 |
| 中间件（ROS2） | 节点加载模型 | [1] 传入 | 整机控制权 |

**一句话威胁模型**：攻击者 = 模型发布者；受害 = 任何加载 .onnx 的机器人；
后果 = 从 DoS（加载崩溃）到 RCE（任意代码执行）到物理危害。

---

## 六、这决定了 fuzz 打哪（下一步）

```
当前：fuzz [1] protobuf 反序列化（已验证能找到崩溃，但多在库层）
下一步：fuzz [2] 真实 onnxruntime 图构建 → 找 ORT 自己的 CVE
再下一步：fuzz [3] shape 推导（整数溢出）—— 历史 CVE 高发区
```

**优先级依据**：第四节的 CVE 映射表填完之后，哪一环 CVE 最密集、修复模式最一致，就优先 fuzz 哪一环。
