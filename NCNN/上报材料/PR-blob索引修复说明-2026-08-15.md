# ncnn 修复 PR 提交包 — `load_param_bin` 层头/索引加固（2026-08-15）

> 对应漏洞：漏洞3（`net.cpp` bin 路径 blob 索引越界写）+ 同函数 count 值未校验（negidx 暴露）
> 提交人：`kaltsit-10`（昵称；不出现真实姓名/组织）
> 状态：**已被 #6922 取代（计划关闭）**。#6902 open，head=`0f3dc15`（五守卫，31+/2-）；
> BIN+TEXT 守卫已统一并入 #6922（head `31341ba`，40+/4-）。
> `afad533` 为 fork 旧分支 commit，**从未进入上游 master**，勿引用。
> 补丁文件：同目录 `PR-blob索引修复.patch`（73 行，五处守卫，head `0f3dc15`）

---

## 一、PR 信息

| 项 | 值 |
|---|---|
| **PR 标题** | `fix out-of-bounds blob index and unvalidated count in load_param_bin` |
| **分支名** | `fix/blob-index-oob`（已在 WSL 本地仓库创建） |
| **基准** | ncnn master `a4d2ea1`（提交前 rebase 到 upstream 当前 main = `e3b6ec9c`；blob 索引区在 a4d2ea1→ed9466b 间未变，补丁应干净套用） |
| **改动** | 仅 `src/net.cpp` `Net::load_param_bin`，+31 / -2（五守卫：header 上限、count 上限、bottom/top 索引） |

---

## 二、PR 描述（body，直接粘贴）

```markdown
## Summary

`ncnn::Net::load_param_bin()` reads attacker-controlled integers from a `.parambin`
model file and uses them (a) to index into `d->blobs` with no bounds check, and
(b) to size `layer->bottoms` / `layer->tops` with no sign check. A crafted file can:

1. set `bottom_blob_index` / `top_blob_index` to any int32 → out-of-bounds read
   and write (`Blob& blob = d->blobs[idx]; blob.consumer = i;`) — CWE-125/787
2. set `bottom_count` / `top_count` to a negative value → `std::vector::resize(-1)`
   throws `std::length_error` → uncaught → `std::terminate()` — DoS

## Changes

- validate `bottom_count` / `top_count` are non-negative before use
- validate `bottom_blob_index` / `top_blob_index` are in `[0, blob_count)` before
  indexing `d->blobs`

## Why

ncnn parses untrusted model files in many embedding scenarios (mobile/desktop apps,
model hubs, WeChat/QQ). On master, a 149-byte crafted `.parambin` drives a
deterministic SIGSEGV (ASan: WRITE @ `src/net.cpp:1810`, `blob.consumer = i`);
a 28-byte file with `top_count = -1` aborts via uncaught `std::length_error`.
Both are fully reproducible and independent of the already-fixed CVE-2026-50144,
whose fix (`5a0288f2`) only guards parameter ids in `src/paramdict.cpp`.

## Test

| input | before (master a4d2ea1) | after |
|---|---|---|
| crafted `bottom_blob_index=0x09090909` (149B) | SIGSEGV (exit 139) | `ret=-1`, no crash |
| crafted `top_count=-1` (28B) | abort (exit 134, `std::length_error`) | `ret=-1`, no crash |
| valid header file | `ret=-1` (ParamDict err) | identical, no false positive |

The `clear(); return -1;` error path matches existing failure handling in this
function.
```

---

## 三、commit message

```
fix out-of-bounds blob index and unvalidated count in load_param_bin

bottom/top blob indices from a crafted .parambin are used to index d->blobs
with no bounds check (CWE-125/787); bottom/top counts are used to resize
layer->bottoms/tops with no sign check (uncaught std::length_error).
validate both before use.
```

---

## 四、提交流程（对照 CVE-2026-50144 报告者 aleff 的先例）

> **2026-08-18 更新**：#6902 已 open（head `0f3dc15`）。因 bot 指出 BIN/TEXT 应合并覆盖，
> 已将 BIN 守卫并入 #6922（rebase 后 head `31341ba`），#6902 **计划关闭**（打开 #6902 → Close pull request，
> 作者有权限，无需 token）。此流程保留作历史记录。

1. **fork** Tencent/ncnn（GitHub）→ `git fetch upstream main` → 把本地 `fix/blob-index-oob` rebase 到 upstream main
2. 用当前 main 重新生成补丁（若上下文偏移，手动套用三处守卫即可）；`git diff` 与 `PR-blob索引修复.patch` 核对一致
3. `git push origin fix/blob-index-oob` → GitHub 开 PR，body 粘贴第二节
4. **推荐顺序**：先提交 08-12 补充上报的 GHSA（漏洞3 私密披露，建立披露记录 + CVE 申请路径），再开 PR；两者可并行，不等 GHSA triage（对应"老师建议直接提交 pr 不等 GHSA"）
5. 合并后维护者走 GitHub CNA 流程 Request CVE（CVE 跟 advisory 生命周期，不跟 commit——之前已确认）
6. PoC 文件不随 PR 提交（保持私密直至修复公开）

---

## 五、验证矩阵（补丁 A/B，实测）

环境：WSL2 Ubuntu 22.04，ncnn `a4d2ea1`，GCC Release（`build_vuln`）。补丁后 `make ncnn` 重链 `loadpoc_file_fixed2`。脚本：`NCNN/fuzz/pocfix/test_fix.sh`

| 输入 | 基线（未打补丁） | 修复版 |
|---|---|---|
| `ncnn_blobidx_oob_149B.parambin` | **SIGSEGV exit 139** | `invalid bottom_blob_index 151587081` → ret=-1 |
| `ncnn_blobidx_oob_negidx.parambin` | **abort exit 134**（`std::length_error`） | `invalid bottom_count or top_count 1 -1` → ret=-1 |
| `ncnn_blobidx_oob_p1idx.parambin` | ret=-1（截断文件，无 OOB） | ret=-1（行为不变） |
| `control_allzero.parambin` | ret=-1 | ret=-1（行为不变） |
| 合法头文件（`c_valid.parambin`） | ret=-1（ParamDict err） | ret=-1（**无守卫误拒**） |

`151587081 = 0x09090909`（首个 bottom_blob_index @ offset 24，blob_count=14080）。

---

## 六、后续可选（另开 PR）

| 漏洞 | 位置 | 补丁方向 |
|---|---|---|
| 漏洞2（text 层头 top_count 越行读取） | `src/net.cpp` `Net::load_param` | 层头 4 字段解析后校验 `bottom_count/top_count` |
| 漏洞1（96B NULL 解引用，bin 数组 len） | `src/paramdict.cpp` `ParamDict::load_param_bin` | `len <= 0` 校验 + `Mat::empty()` 检查 |

需要的话可按本包模板继续准备这两份 PR。

---

## 附：与 GHSA 的衔接

- 漏洞3（本 PR）的 GHSA 提交材料 = `NCNN-bin-blob索引-补充上报-2026-08-12.md`（已修正字节布局）
- 已在 triage 的 2 个 GHSA（96B 促修复 + text 层头）不受本 PR 影响；若维护者后续就 96B/text 开 PR，可同样关联
