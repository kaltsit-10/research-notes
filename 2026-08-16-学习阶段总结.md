# 2026-08-16 学习阶段总结（08-12 → 08-16）

> 阶段：从"发现 blob 越界漏洞"到"首个被厂商合并的修复 PR + CNVD 实名报送"，并顺着修复挖出 3 个 TEXT 平行漏洞。
> 覆盖 08-12 至 08-16，核心事件 5 个：**blob 漏洞发现 → 修复 PR 合并 → 覆盖率 fuzz 重建 → TEXT F1/F2/F3 平行漏洞 → CNVD 渠道与材料落地**。另完成全目录产品化重排。

---

## 核心收获（本阶段四件事）

1. **blob 漏洞全流程走通（本课题第一个被合并的修复）**：`Net::load_param_bin` blob 索引越界 → 149B PoC 双向验证 → 修复 commit `afad533` 被 ncnn 合并 → 改走 **CNVD 实名渠道**报送
2. **"补丁 diff 挖法"同项目二次应验**：afad533 只修 BIN 漏修 TEXT → 顺着同一层头 token 校验缺口挖出 **F1/F2/F3 三个平行漏洞**（净得 3 个未公开新漏洞）
3. **覆盖率引导 fuzz 是深入的关键**：cov 从 227 跳到 1001 后 fuzzer 才真正打进 layer 解析路径；guard 必须逐字节镜像 scanf 语义否则反复发现已知 DoS
4. **CNVD 报送机制落地**：通用型漏洞表单逐字段填报 + 报送 zip 包（PoC 打 zip 规避附件类型限制）；实名注册/署名决策明确

---

## 一、阶段时间线（5 个里程碑）

| 日期 | 关键事件 | 产出 |
|------|---------|------|
| 08-12 | **blob 漏洞发现**：`load_param_bin` 解析 `.parambin` 时 bottom/top blob 索引为攻击者可控 int32，无 `0<=idx<blob_count` 校验直接 `d->blobs[idx]` → CWE-787 越界写 + CWE-125 越界读 | `NCNN/fuzz/发现-NCNN-bin-blob索引越界-2026-08-12.md`、149B PoC |
| 08-15 | **修复 PR 合并**：`afad53357b60536b0a6d8f91fee83b7ae32dd22d` 13:19 合 master HEAD（未发布），受影响版本 ≤ tag 20260526；修复前 SEGV WRITE @ net.cpp:1810 / 修复后优雅拒绝，双向实测 | `NCNN/上报材料/PR-blob索引修复说明-2026-08-15.md` |
| 08-15 | **覆盖率 fuzz 重建**：libncnn.a 用 inline-8bit-counters 重编（clang-15 弃用 trace-pc-guard）；guard 逐字节镜像 scanf 语义；修 run_cov_loop 的 `set -e` 陷阱 | `NCNN/fuzz/{build_cov.sh,run_cov_loop.sh,fuzz_param.cc}` |
| 08-15 | **TEXT 平行漏洞 F1/F2/F3**：三个都是 afad533"只修 BIN 漏修 TEXT"的平行问题，master 未修 | `NCNN/fuzz/pocs_text_layer/`（26B/24B/38B PoC） |
| 08-16 | **CNVD 渠道决策 + 材料落地**：maintainer 不响应 → GHSA 走不通 → 选 CNVD 实名渠道（用户拍板实名注册+实名署名）；通用型表单逐字段填报 + 报送 zip 包 | `NCNN/fuzz/CNVD-blob漏洞报送材料.md`、`NCNN/fuzz/cnvd_submission/` |

---

## 二、成果清单（最终状态，截至 08-16）

| # | 漏洞 | CWE | 公开状态 | 状态 | 去向 |
|---|------|-----|---------|------|------|
| 1 | ⭐ **NCNN blob 索引越界**（load_param_bin bottom/top 索引无 blob_count 校验 → `d->blobs[idx]`） | 787+125 | **已修**（afad533 合并，≤20260526 受影响，未发布） | 报送中 | **CNVD 实名渠道**（CVSS≈6.1 中危） |
| 2 | ⭐ **TEXT F1**：MVN bottom 越界读（net.cpp:1443 无 `blob_index<blob_count` 检查） | 125/787 | **master 未修**，未公开 | 待报 | MITRE（别名）/ 等厂商 |
| 3 | ⭐ **TEXT F2**：top_count=-1 → `tops.resize(-1)` → std::length_error → abort | 248/755 | **master 未修**，未公开 | 待报 | 同上 |
| 4 | ⭐ **TEXT F3**：YoloDetectionOutput 构造函数未初始化裸指针 `softmax` → Net 析构 destroy_pipeline 解引用垃圾指针 | 457/476 | **master 未修**，未公开 | 待报 | 同上 |

**TEXT 层头格式坑（解析顺序）**：`type name bottom_count top_count` 之后才是 bottom 名 / top 名 / param_count——不是 bottom 名先于 top_count，写错会报 "parse top_count failed"。

**crash_cov 分类全貌**：F1(29) / F2(161) / known_dos(3+96B 变体) / other 只剩 F3；BIN 70B crash 是已知 96B NULL-deref 变体（paramdict.cpp:594），非新。

---

## 三、关键决策与转折（含教训）

### 1. PR 被接受 ≠ CVE 到手（08-16，渠道重新评估）
> 合并了也不代表编号自动来。申请 CVE 的通道逐个评估：
- **GHSA**：需 maintainer 权限（维护者建 advisory），用户判断大概率不会理
- **MITRE 兜底**（cveform.mitre.org）：无 CNA 覆盖或 CNA 不响应时可填，但 GitHub 作为 CNA 可能被打回；且需中性邮箱
- **CNVD（最终选定）**：用户拍板"实名注册+实名署名"，仅限此 blob 漏洞；其他渠道仍用 `kaltsit-10` 别名
- 教训：**成果出口要提前想清楚**——"能不能拿编号"取决于渠道与权限，不取决于漏洞真实性

### 2. CNVD 表单类型坑（08-16）
> 用户最初贴的是"事件型（网站）漏洞"表单（URL/IP/sqlmap/WEB 字段），与 blob 漏洞完全对不上。
- **通用型漏洞表单**才看得到 厂商/产品/版本/CWE/CVSS 字段
- 附件类型限制：只收 `.doc/.docx/.zip/.py` → **`.parambin` 必须打 zip**

### 3. 修复只堵一个口子 → 顺着挖平行漏洞（08-15，本阶段最大方法论验证）
> afad533 在 `load_param_bin`（BIN 路径）加了 `bottom_count/top_count` 校验，但 **text 路径 `load_param`（net.cpp 层头）的 token 数校验完全没碰**。
- 同一缺陷两个入口：BIN 修了 → 试 TEXT → 一口气挖出 F1/F2/F3
- 教训：拿到修复 commit 的第一反应应该是 **"修了哪条路径？没修哪条？"**——平行路径就是漏洞金矿

### 4. 覆盖率是 fuzz 有效性的硬指标（08-15）
> 之前 trace-pc-guard 被 clang-15 弃用导致 fuzz 一直在表层空转；换 `inline-8bit-counters,pc-table,trace-cmp,indirect-calls` 后 cov 227→1001，fuzzer 才真正深入 layer 解析路径并出 F1/F2/F3。

### 5. guard 逐字节镜像 scanf 语义（08-15，防"已知漏洞反复发现"）
> guard 不镜像 glibc scanf 的语义，fuzzer 会反复通过变体重现已知 CWE-789 DoS，每次都是一次 libFuzzer 进程重启：
- 负数计数超 int32 → scanf %d **环绕成正数**，绕过 `<=0` 检查
- 大负数需保留 mag 低 32 位（`& 0xFFFFFFFF`），**不能饱和**
- `\v`/`\f` 也是 scanf 空白 → skip_ws 必须含
- magic 可带前导 `+` → 用 parse_int32 而非 memcmp
- count 有效 int32 值 `> 65536` 才 reject

### 6. `set -e` 陷阱（08-15）
> `run_cov_loop.sh` 的 `set -e` 会在 fuzzer 首次崩溃/OOM（非零退出）时杀死整个循环——之前看到的"多轮 run"全是手动重启伪装。必须 `set -uo pipefail`（去 e），靠 `rc=$?` 捕获。

### 7. 目录产品化重排（08-16）
> 原 `复现实验/` 单一大目录过时臃肿 → 按产品根分类：`NCNN/ MINDSPORE/ AGIBOT/ ONNX_ORT/` + `研究文档/ 脚本工具/ archive/`。删除 3 个垃圾文件（VS Code 连接缓存、`tensor_py_210.cc`），md5sum 验证判定 AgibotX1 旧草稿为**不完整版本**归档而非硬删，总览文档 19 处失效链接全部修复。

---

## 四、方法论沉淀（本阶段增量）

```
1. 双向验证（A/B 对照）：修复前 SEGV WRITE @ net.cpp:1810 / 修复后 "invalid bottom_count"
   优雅拒绝——上报前必须双向实测，不能只测崩溃侧
2. 补丁 diff 挖法·二次应用：拿到修复 commit → 问"修了 BIN 漏了 TEXT？"→
   平行路径 F1/F2/F3 = 同缺陷另一入口，比从零挖快
3. 覆盖率引导：clang-15 必须用 inline-8bit-counters（trace-pc-guard 已弃用）；
   cov 数量级跳变 = fuzz 有效性转折点
4. 语义镜像 guard：fuzz 前置过滤器必须逐字节复刻解析器语义（scanf 环绕/空白/前导符），
   否则 fuzzer 反复撞已知 bug 无法深入
5. 诚实边界：149B PoC 写值受限（blob.consumer=i）→ 评级保守（CVSS≈6.1 中危），
   不虚高；修复后构建（fuzz_param_cov 含 afad533）实测不崩溃
6. 取证纪律：PoC 修复前保密（blob 已修可交 CNVD 验证；TEXT F1/F2/F3 仍保密）
```

**阶段级洞察**：ncnn 模型解析是**两个并行解析器**（BIN `load_param_bin` + TEXT `load_param`），安全修复经常只打一个入口——**每次拿到 ncnn 修复 commit，先 diff 两个解析器路径是否都打了**。这是 ncnn 这类双格式解析器的高产出挖洞模式。

---

## 五、诚实审计与纠偏记录（本阶段）

| 项 | 原判断 | 纠偏后 | 依据 |
|----|--------|--------|------|
| 149B PoC 崩溃形态 | SEGV WRITE | **SEGV WRITE @ net.cpp:1810（blob.consumer=i）** | ASan + 修复后对照 |
| CNVD 报送目标 | 曾误向 TEXT 路径 F1/F2/F3 | **必须已合并的 blob 漏洞**（afad533） | 用户纠正"走 blob 的 CNVD，因为这个被合并了" |
| AgibotX1 旧材料 | 初判可删 | **不完整旧草稿，归档** | md5sum 字节不一致（asan_poc2 空文件） |
| 署名 | 统一 kaltsit-10 | **CNVD blob 报送改实名**（仅限此例） | 用户拍板实名注册+实名署名 |
| CNVD-2026-23196 | — | **不引用**（疑似串号捏造） | 沿用 08-07 结论，串号 CVE 实为 Linux HID |

**上报身份红线（全程遵守）**：CNVD 实名仅限 blob 漏洞；其他渠道 `kaltsit-10`；不出现真实姓名/导师课题组字眼/虚构组织；QQ 邮箱不进上报；TEXT F1/F2/F3 修复前不公开。

---

## 六、当前状态与下一步

### 等待中
- **CNVD 报送**：材料已全部备齐（`NCNN/fuzz/cnvd_submission/`），等用户自行注册提交；CNVD-ID → CNCERT 推 CVE
- **ncnn coverage campaign**：WSL 持续运行，session 内 cron（:07/:37）查新崩溃产物并去重

### 下一步候选（按价值排序）
1. **TEXT F1/F2/F3 上报**：master 未修未公开，走 MITRE（别名）或等厂商；须先确认与 afad533 的修复状态独立
2. **ncnn 双解析器 diff 纪律化**：每次新修复 commit 先 diff BIN/TEXT 两条路径，沉淀为固定审计步骤
3. **agibot 上报渠道实机确认**：PVR 是否开启、仓库无 SECURITY.md——待用户决定
4. **收尾**：PAT 撤销（ncnn-push）；ncnn vuln 1（paramdict len）与 vuln 2（text layer header）独立 PR；跟进 CNVD 审核

---

## 附：本阶段关键文件索引（新重排路径）

| 文件 | 内容 |
|------|------|
| `NCNN/fuzz/发现-NCNN-bin-blob索引越界-2026-08-12.md` | blob 漏洞完整发现文档（149B PoC 布局 + 双向验证） |
| `NCNN/fuzz/CNVD-blob漏洞报送材料.md` | CNVD 报送完整材料（厂商/版本/CWE/CVSS≈6.1/修复建议） |
| `NCNN/fuzz/cnvd_submission/` | 报送 zip 包（报送材料 + 149B parambin + ASan 证据） |
| `NCNN/上报材料/PR-blob索引修复说明-2026-08-15.md` | afad533 修复前后对照 + 补丁说明 |
| `NCNN/上报材料/NCNN-bin-blob索引-补充上报-2026-08-12.md` | 补充上报材料 + SHA-256 全集 |
| `NCNN/fuzz/pocs_text_layer/` | TEXT F1/F2 最小 PoC（26B/24B）+ F3（38B） |
| `NCNN/fuzz/{fuzz_param.cc,build_cov.sh,run_cov_loop.sh}` | 覆盖率引导 fuzz 工具链（inline-8bit-counters） |
| `研究文档/漏洞归档总览-2026-08-07.md` | 跨产品主索引（链接已全部修复） |
| `研究文档/笔记/2026-08-12-学习阶段总结.md` | 上一阶段（07-26→08-12）总结 |
