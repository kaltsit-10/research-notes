# 2026-08-22 学习阶段总结（08-18 → 08-22）

> 阶段主题：CNVD 材料重写与补证、AGIBOT/AimRT/宇树 XR 三线漏洞挖掘与上报准备、国产具身智能组件陌生 PR 友好度调研纠偏、研究路线转向"自动化修复正确性验证"。
>
> 研究身份与边界：本人是网络空间安全专业学生，当前进行授权的本地、离线安全漏洞挖掘学习与研究。不连接真实执行器，不进行未授权网络投递，不研究 RCE 利用链；未经导师进一步讨论和授权，不提前开展 L3/L4 攻击影响证据搜集。（XR 组件上报材料中有 L3 本地模拟实验，全程未连接真实机器人执行器，且已在材料中声明授权范围。）
>
> ⚠️ 本阶段重要纠偏：**国产组件调研 v1 结论（AimRT/宇树 XR 高优先级）被 v2 实测推翻**——详见第三节；**自动化检测/修复方向经文献调研确认属"学术红海"，但"修复正确性验证闭环"是未做扎实的组合增量**——详见第五节。

---

## 一、核心收获（本阶段五件事）

1. **CNVD 材料重写 + OBS 补证全流程**：修正"6902 已合并"的误判口径（实为 closed 未合并；上游 master 未修复，修复在 fork PR #6922 待合入）；按 CNVD 补证要求重写报送材料（代码分析/验证步骤/截图/视频四要素）+ 生成 Word 文档 + OBS 录制脚本 + 复现视频全流程。
2. **AGIBOT/AimRT 双线漏洞挖掘落地**：agibot_x1_infer 14 个首方缺陷 + AimRT 4 个中间件缺陷（去重后 18 个独立 finding），其中 AR-1（JSON→ROS2 越界写）已备好英文 PR body，x1_infer 平行数组族有 ASan PoC。
3. **宇树 XR 遥操作栈 6 个漏洞（含 L3 实测）**：无认证控制通道、DDS 无加密、视频流泄露、限速绕过等——是当前攻击价值最高的组件（L3 本地模拟已实测），已备 GHSA/PR 双通道材料。
4. **国产组件陌生 PR 友好度调研 v2 纠偏**：v1 凭 star 数高估 AimRT/宇树 XR；v2 用 GitHub API 拉取 PR 列表实测（外部贡献者比例/合并率/时间跨度），将 AimRT 降级（外部多为内部员工账号）、剔除 OpenLoong/genie_sim，修正 Top 5 为 MNN/lerobot/pymycobot/FastDeploy/TNN。
5. **研究路线转向防御侧**（导师建议）：从"找可升级到任意代码执行的漏洞"转向"设计自动化检测与修复特定类型漏洞"；经文献调研确认方向为学术红海，但"修复正确性验证闭环（恶意+合法双向回归）"是未做扎实的组合增量，且可升级为 DSH 全链自动化工作流。

---

## 二、阶段时间线

| 日期 | 关键事件 | 产出 |
|------|---------|------|
| 08-18 | NCNN 复核收尾、PR #6902/#6922 状态核验、CNVD 材料口径修正（续 08-18 笔记） | `NCNN/上报材料/NCNN-漏洞复核收尾-2026-08-18.md` |
| 08-19~20 | DSH 环境搭建：pnpm 缺失→npm 全局安装；`dsh plugin add dsh-pentest` 安装并验证加载 | web profile 注册 `@howmp/dsh-pentest`（渗透模式预设） |
| 08-20 | CNVD 材料重写：修正合并状态误判、新增在线表单逐字段对照、Word 文档生成、OBS 录制流程写入、冗余文件清理 | `NCNN/fuzz/cnvd_submission/报送材料.md`（重写版）、`CNVD-ncnn-blob索引越界写-漏洞报告.docx`、`record_repro_video.sh`、v2 zip |
| 08-20 | **XR 漏洞挖掘完成**：6 个漏洞（无认证控制/DDS/视频/限速/pickle/IPC） | `xr_teleoperate/XR_TELEOPERATE_上报材料/`（L3 实测日志在 WSL） |
| 08-20 | 国产组件调研 v1（乐观版，后被 v2 取代） | `研究文档/国产具身智能开源组件调研-2026-08-20.md` |
| 08-22 | **AGIBOT/AimRT 总览完成**：18 个独立缺陷 + 分 PR 策略 | `AGIBOT/上报材料/AGIBOT-漏洞上报总览-2026-08-22.md`、`PR-aimrt-json_convert-越界写-2026-08-22.md` |
| 08-22 | **调研 v2 纠偏**：GitHub API 实测 PR 活跃度，推翻 v1 乐观评级 | `研究文档/国产具身智能组件陌生PR友好度调研v2-2026-08-22.md` |
| 08-22 | 防御侧方向讨论 + 文献调研：补丁正确性验证（Patch Correctness Assessment）是活跃子领域 | 见第五节（本笔记） |

---

## 三、国产组件陌生 PR 友好度调研 v2（重要纠偏）

### 3.1 v1 的错误与用户实测反馈

v1 仅凭 star 数与零散 PR 先例评级，未量化 PR 活跃度与外部贡献者比例，导致 **AimRT / 宇树 XR 被高估**。用户实测挖掘后发现：**AimRT 和宇树 XR 的 PR 很少、且大多是自己人提交的**。

### 3.2 v2 实测方法（可复现）

```
GET https://api.github.com/repos/{owner}/{repo}   # 基本信息
GET .../pulls?state=closed&per_page=50&sort=updated&direction=desc  # 最近 50 条 closed PR
```
判定"外部贡献者"= `author_association` ∈ {CONTRIBUTOR, NONE, FIRST-TIME}（排除 OWNER/MEMBER/COLLABORATOR），并人工核验高频作者是否为组织员工个人账号。

### 3.3 实测结果（2026-08-22）

| 仓库 | stars | 最近50条closed PR | 外部作者 | 外部已合并 | fix/security类 | 判定 |
|---|---|---|---|---|---|---|
| **alibaba/MNN** | 15,944 | 50 | 50 | **49** | 27 | ★★★★★（当天活跃） |
| **huggingface/lerobot** | 26,822 | 50 | 32 | 16 | 20 | ★★★★★ |
| **PaddlePaddle/FastDeploy** | 3,709 | 50 | 49 | 24 | 12 | ★★★★☆（需签 CLA） |
| **elephantrobotics/pymycobot** | 209 | 50 | 50 | 35 | 30 | ★★★★☆（MIT，响应快） |
| **Tencent/TNN** | 4,647 | 50 | 50 | 40 | 17 | ★★★★☆（⚠️2025-05 停更） |
| **AimRT/AimRT** | 1,386 | 50 | 34 | 26 | 7 | ★★☆☆☆（外部多为内部员工账号，2026-04 停更） |
| **unitreerobotics/xr_teleoperate** | 1,625 | 21 | 21 | 12 | 7 | ★★☆☆☆（2 年仅 21 条） |
| **unitreerobotics/unitree_ros2** | 804 | 15 | 15 | 8 | 1 | ★★☆☆☆（2 年仅 15 条） |
| **OpenLoong** | 31 | 4 | 4 | **0** | 0 | ☆☆☆☆☆（无外部合并） |
| **genie_sim** | — | 三个候选组织下均不存在仓库 | — | — | — | ❌ 未找到 |

### 3.4 AimRT "外部作者"身份核验（用户观察的印证）

最近 50 条 closed PR 中，所谓"外部作者"高频出现：
- `owny990312`（多次提交 mqtt/executor/test 报告等核心功能）、`s1mplezy`（多次提交 header/plugin 等核心架构）→ **高度疑似智元员工个人账号**；
- 真正像外部的仅 `tqolf`（NONE 关联，fix RPC timeout）等个别。

### 3.5 修正后 Top 5（面向"陌生 bug-fix PR 能被合并"）

1. **alibaba/MNN**（外部合并率 98%、当天活跃、27 条 fix 先例、与 NCNN 同构方法论可复用）
2. **huggingface/lerobot**（非国产但全球最活跃，外部身份多样）
3. **elephantrobotics/pymycobot**（MIT、响应快、fix 合并多）
4. **PaddlePaddle/FastDeploy**（活跃、含 supply-chain 安全 PR、需 CLA）
5. **Tencent/TNN**（同厂对照、含越界修复先例、停更风险需确认）

---

## 四、漏洞矩阵全景（5 组件 28+ 缺陷，共性与攻击性）

### 4.1 漏洞清单汇总

| 组件 | 数量 | 核心类型 | 关键状态 |
|---|---|---|---|
| NCNN（腾讯） | 7 线（A-G） | 模型文件解析：blob 索引越界写、层头 count 未校验、除零、参数 id 越界 | 上游未修，修复在 fork PR #6922 待合入 |
| MindSpore（华为） | 2 + 变体矩阵 | .ckpt 解析：dims 不校验 → DoS + 堆溢出（CWE-787） | 2.8/2.10/master 均未修 |
| agibot_x1_infer（智元） | 14（X1-1~14） | 配置/消息处理：命令注入、平行数组越界、索引越界、除零 | 2025-04 后 16 个月无提交 |
| AimRT（智元） | 4（AR-1~4） | JSON→ROS2 越界写、HTTP 头注入 DoS、配置解析越界 | 活跃，PR 通道首选 AR-1 |
| xr_teleoperate（宇树） | 6（VUL-01~06） | 无认证控制、pickle RCE、DDS 无加密、视频泄露、限速绕过 | 部分 L3 已实测，物理影响待实机 |

### 4.2 跨组件共性（本阶段最重要的归纳）

**共性 1：输入长度/索引与容量不校验（占 ~60%）**——NCNN `d->blobs[idx]` 无范围校验、MindSpore content 长度与 dims 不互检、AGIBOT 平行数组不互检、AimRT `WriteMemberNested` 缺 array_size_ 检查、XR thumbstickValue 无 clamp。

**共性 2：文本/二进制、新旧路径修复不对称（"兄弟漏洞"）**——NCNN paramdict 修了 net.cpp 没修；MindSpore bf16 分支有界 else 分支裸奔；AimRT `WriteMember` 有检查 `WriteMemberNested` 没有。

**共性 3：安全函数被误用/绕过**——MindSpore `memcpy_s` 第二参数传 src 长度而非 dst 剩余；XR `pickle.load` 反序列化；AGIBOT `system()` 拼 shell。

**共性 4：默认配置下的可达性分层**——NCNN/MindSpore/XR VUL-01/03/04/05 默认即达；AGIBOT X1-4/5/7、AimRT AR-1 配置后可达；XR VUL-02/06 本地前提。

### 4.3 攻击性评估（严格分级，区分已证实/推断）

| 级别 | 漏洞 | 依据 |
|---|---|---|
| 🟢 可直接投入攻击（L3 实测） | XR VUL-01（无认证控制链路）、VUL-03（DDS 伪造）、VUL-04（视频泄露）、VUL-05（限速 500x 绕过）；MindSpore 堆溢出（fastbin 污染确定性崩溃） | L3 本地模拟实测日志 |
| 🟡 可投入验证性攻击 | AimRT AR-1（远程 JSON→ROS2 越界写）、NCNN blob 越界写、AGIBOT X1-3 | ASan/源码触发链确认 |
| 🔴 利用链不完整 | XR VUL-02 pickle（投递路径未闭环）、MindSpore RCE（对抗验证 UNLIKELY，safe-linking 硬墙）、AGIBOT X1-1（默认休眠） | 静态确认/对抗验证 |

**诚实边界**：全部物理影响（执行器动作）待实机验证；RCE 一律不主张（材料中已声明）；上报按"内存破坏/DoS"实评不虚高。

---

## 五、研究路线转向：自动化修复（导师建议的落地评估）

### 5.1 导师原话要点

> "找能升级到任意代码执行级别的漏洞本身就很难，而且做漏洞利用问 AI 还会被安全护栏限制。能不能从防御者角度设计自动化检测和修复某一种特定类型（内存越界、整数溢出、逻辑错误）的 bug 或漏洞？"

### 5.2 文献调研结论（诚实评估，不讨好）

**检测（SAST）是 20 年以上成熟红海**：Coverity（2002）、Fortify（2001）、CodeQL、cppcheck、Clang Static Analyzer、Infer、Semgrep 全部成熟，oss-fuzz 免费托管持续 fuzz。**"设计自动化检测越界"= 重新造 cppcheck/CodeQL 子集，无学术增量。**

**修复（APR）2009 年就开始，LLM 时代已井喷**：GenProg（2009）→ RepairBench（2025）、SWE-bench、Google CodeMender、GitHub Copilot Autofix 已产品化。"LLM 修 CWE 特定类型"已是论文红海。

**但"修复正确性验证闭环"是未做扎实的组合增量**（关键证据）：

| 文献 | 发现 |
|---|---|
| [Patch Correctness Assessment: A Survey](https://dl.acm.org/doi/10.1145/3702972)（TOSEM 2025） | 补丁正确性评估是活跃子领域，现有方法均有短板 |
| [When APR Meets Regression Testing](https://dl.acm.org/doi/10.1145/3672450)（TOSEM 2024，200 万补丁） | **传统回归测试不足以判定补丁正确性**（恶意输入不在测试集） |
| [Poracle](https://dl.acm.org/doi/10.1145/3625293)（TOSEM 2023） | "合法输入行为不变"（preservation condition）对抗补丁过拟合 |
| [Unveiling Shortcomings of Patch Overfitting Detection](https://arxiv.org/html/2603.11262v1#3)（2026） | 现有过拟合检测方法都有实践缺陷 |
| [Fixing Security Vulns with AI in OSS-Fuzz](https://arxiv.org/abs/2411.03346)（2024） | Google 官方在用 fuzz 验证 AI 修复（同向印证） |
| [EAIR Software Bugs 实证](https://arxiv.org/abs/2507.18267) | 具身机器人软件缺陷实证是较新方向（垂直增量） |

**结论**：A+B 方向（恶意输入拒绝验证 + 合法输入不误拒验证，用真实 PoC + ASan 双向回归）是现有方法（合成测试/静态 oracle/LLM 判别）的**组合增量**——学术增量中等，工程增量高；且与已有资产（13 组 ASan A/B 矩阵、PoC 集、fuzz harness）直接协同。

### 5.3 可升级为 DSH 全链工作流（第三条→第四条）

```
DSH agent 工作流（防御侧闭环）
1. detect    静态/动态扫描（cppcheck/ASan/fuzz + 专属 Semgrep 规则）
2. locate    定位 CWE-787/125/190/369 + 行号
3. repair    规则模板生成补丁（补一行检查，与 NCNN/AimRT 修复同构）
4. verify    ⭐ 双向 ASan 回归（A: PoC 集必须 ret=-1 无崩溃；B: 合法输入必须 ret=0 无误拒）
5. report    生成补丁 + 验证证据 → 提 PR
```

与现有 DSH 安全插件定位区分：`dsh-skill-pack-security`=审计方法论教学（无修复无验证闭环）；`dsh-plugin-cyber`=渗透工作流（偏攻击侧）；本方案=防御侧"检测→修→验→PR"闭环。

---

## 六、诚实审计与纠偏记录（本阶段）

| 项 | 原判断 | 纠偏后 | 依据 |
|----|--------|--------|------|
| PR #6902 合并状态 | 曾误记为"已合并" | **closed，从未合并**（merged=False） | GitHub API 2026-08-20 实测 |
| 上游修复状态 | 曾写"已修复" | **上游 master 946fe3f 未修复**；修复在 fork PR #6922 待合入 | git merge-base + API 核验 |
| 国产组件调研 v1 | AimRT/宇树 XR 高优先级 | **降级**（PR 少且多为内部提交） | v2 实测 + 用户挖掘反馈 |
| genie_sim 仓库 | 曾列第四候选 | **未找到官方仓库**（三个候选组织均不存在） | GitHub API 核验 |
| "自动化检测/修复"方向 | 曾称"你的机会"（讨好） | **学术红海**；真正增量在"修复正确性验证闭环" | 文献调研（TOSEM 2025/2024 等） |
| MindSpore 堆溢出评级 | 曾疑可升级 RCE | **RCE UNLIKELY**（safe-linking + 无泄漏原语硬墙）；报"可信内存破坏" | 对抗验证（08-07 已完成） |

---

## 七、当前状态与下一步

### 等待中
- **CNVD blob 报送**：材料重写+Word+OBS 流程已备齐，**待补录复现截图与视频**（OBS 录制，画面须含 ncnn + 20260526），然后用户自行提交
- **NCNN PR #6922**：open 待上游维护者 review/合并
- **XR/AGIBOT 上报**：材料已备，待用户与导师确认渠道（GHSA vs PR）与授权边界

### 下一步候选（按价值排序）
1. **防御侧原型**：用 NCNN 漏洞集做"修复正确性验证闭环"最小原型（检测→补丁→双向 ASan 回归），证明 A+B 方向可行性后定论文定位
2. **CNVD 补证**：按 `record_repro_video.sh` + OBS 录制视频/截图，补入报送包后提交
3. **XR/AGIBOT 上报启动**：与导师确认授权边界后，AimRT AR-1 走 PR（活跃）、x1_infer/XR 走 GHSA（私密）
4. **调研文档整理**：13 篇 Patch Correctness 文献摘要入库（`研究文档/`），为论文 Related Work 打底

---

## 附：本阶段关键文件索引

| 文件 | 内容 |
|------|------|
| `NCNN/fuzz/cnvd_submission/报送材料.md` | CNVD 报送材料重写版（事实更正+代码分析+验证步骤+OBS 流程+表单对照） |
| `NCNN/fuzz/cnvd_submission/CNVD-ncnn-blob索引越界写-漏洞报告.docx` | Word 版报送报告（60+ 段，含表单表） |
| `NCNN/fuzz/cnvd_submission/record_repro_video.sh` | OBS 录制辅助脚本（绝对路径版） |
| `xr_teleoperate/XR_TELEOPERATE_上报材料/` | XR 6 漏洞上报材料（L3 实测） |
| `AGIBOT/上报材料/AGIBOT-漏洞上报总览-2026-08-22.md` | AGIBOT/AimRT 18 缺陷矩阵 + 分 PR 策略 |
| `AGIBOT/上报材料/PR-aimrt-json_convert-越界写-2026-08-22.md` | AimRT AR-1 英文 PR body（推荐首发） |
| `研究文档/国产具身智能组件陌生PR友好度调研v2-2026-08-22.md` | 组件调研 v2（纠正 v1） |
| `研究文档/上下文交接-2026-08-18.md` | 上一阶段交接文档（已更新 NCNN 报送材料路径引用） |
