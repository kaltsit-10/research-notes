# 2026-08-05 学习笔记

## 核心收获
1. **AI 审计 onnx 包**：11 条历史 CVE 在 1.22.0 **全部干净**（实证测试，诚实结论）
2. **打通 fuzz 真实 ORT 引擎**：解决 **10 个构建障碍**，编译出带 ASAN 的真实引擎 fuzzer，循环已跑起来
3. **认知修正**：历史漏洞模式的路走不通 → 找新漏洞必须靠**新 fuzz**，不靠找历史变体

---

## 一、AI 审计结果（实证测试，脚本可复跑）

| 审计对象 | 对照 CVE | 1.22.0 结果 |
|---------|---------|------------|
| external_data 加载 | 7 条路径遍历 | ✅ 全 BLOCKED（../、绝对路径、symlink、hardlink 全挡） |
| version_converter | Gemm/Upsample 2 条 | ✅ 已修复（断言拦截） |
| onnx.hub 下载 | 2 条 | ✅ **代码已整体移除** |

**结论**：onnx 包 11 条历史 CVE 全部处置完毕。修复质量高（三层防御 + 内核级 openat2 + inode 对比）。

> 测试脚本：`audit_external_data_test.py`、`audit_version_converter_test.py`
> 完整报告：[审计报告-2026-08-05-external数据与version_converter.md](审计报告-2026-08-05-external数据与version_converter.md)

---

## 二、战略含义

```
历史 CVE 模式（路径遍历/断言拦截）→ 已死，别浪费时间
新漏洞的来源 → 对 C++ 代码做新 fuzz（ASAN + 覆盖率）
onnx 包 → 拿 CVE 的主战场（但已知模式已干净，靠新发现）
ORT 引擎 → MSRC 披露 + 论文素材（不发表 CVE）
```

---

## 三、ORT fuzz 构建：10 个障碍全记录

| # | 障碍 | 解法 |
|---|------|------|
| 1 | cmake 3.22 太旧（需 3.28+） | `pip install 'cmake==3.31.10'`（官方源） |
| 2 | main 分支 protobuf 补丁打不上 | `git checkout v1.28.0`（release 依赖锁定） |
| 3 | 系统 libonnx/libprotobuf 头污染 | `apt remove` 旧 dev 包 |
| 4 | AVX512-FP16 汇编（clang14 不认识） | 装 `clang-15` |
| 5 | 依赖 zip 下载损坏 ×N | `curl + SHA1` 预下载全部 41 个依赖到镜像 |
| 6 | cmake 4.3 zip 解压 bug | 降级 cmake 3.31 |
| 7 | configure 超时 | 后台任务跑，不设内部 timeout |
| 8 | AVX-NE-CONVERT asm（clang15 也不认识） | **F16C 补丁** `patch_cvtfp16.py` |
| 9 | structured binding 被 lambda 捕获（C++17/20 都禁止） | **C++20 + 补丁** `patch_model_editor.py` |
| 10 | protobuf-mutator 是 git submodule（网络 clone 失败） | **curl 下载 commit 源码**放置 |

**成果**：主库 100% 构建 + `onnxruntime_libfuzzer_fuzz`（17MB，`-fsanitize=fuzzer,address` ASAN 确认）。

> 构建细节已存记忆 `ort-fuzz-build-setup.md`，镜像脚本 `download_deps_mirror.sh`

---

## 四、ORT fuzz 循环：当前状态 + 诚实警告

```
状态：独立进程运行中
种子：5846 个 onnx 文件
速度：~16,000 exec/s（大部分输入在解析期就失败）
已执行：210 万次+，无 crash
目标：onnxruntime_libfuzzer_fuzz（真实 Ort::Session 引擎）
```

**⚠️ 诚实警告**：`WARNING: no interesting inputs` + `corp: 1/1b` —— **覆盖率不增长**。
原因：
1. `OrtLibfuzzer.cpp` 用 `catch(...)` 吞掉所有异常
2. 随机字节变异很难产生结构合法的 ONNX 模型 → 进不了引擎深处

**它仍然能抓崩溃**（ASAN 兜底），但主要靠"撞"不是"覆盖引导深挖"。
**优化方向**（下一步）：用 protobuf-mutator 结构化 harness（`onnxruntime_proto_libfuzzer` 是候选）。

---

## 五、今日文件清单

| 文件 | 作用 |
|------|------|
| `审计报告-2026-08-05-*.md` | 审计完整报告 |
| `audit_external_data_test.py` / `audit_version_converter_test.py` | 实证测试脚本 |
| `patch_cvtfp16.py` / `patch_model_editor.py` | 构建补丁 |
| `download_deps_mirror.sh` | 依赖镜像下载脚本 |
| `run_ort_fuzz.sh` | ORT fuzz 循环脚本 |
| 记忆 `ort-fuzz-build-setup.md` | 10 个障碍解法存档 |

---

## 六、关键突破：fuzz 引擎插桩修复（下午）

### 发现
`onnxruntime_libfuzzer_fuzz` 能跑但 **coverage 不增长**（corp: 1/1b）。根因：**libonnxruntime.so 没编译进 ASAN/覆盖率插桩**——引擎代码对 fuzzer 完全不可见，所有输入塌缩成 1。

### 修复（OSS-Fuzz 标准配置）
```
库编译:  -fsanitize=fuzzer-no-link,address   (coverage hook 变弱符号)
fuzzer:  链接 -fsanitize=fuzzer,address     (提供真实 runtime)
链接器:  -fuse-ld=lld                        (解决 DWARF5 + GNU ld 不兼容)
构建:    setarch -R 下跑                     (解决 ASAN shadow/protoc 崩溃)
补丁:    去掉 onnxruntime.cmake 的 --no-undefined (允许 .so 未定义符号，运行时由 exe 提供)
```

### 验收结果（用户要求"确定能跑了再结束"）
```
cov:   713 → 24504（34 倍增长）
corp:  147 → 1212+（语料库持续积累）
ft:    34486
exec/s: 209（每次都是真实引擎执行，深度到达 inference_session）
ASAN:  active
```

**当前运行**：`onnxruntime_proto_libfuzzer`（protobuf-mutator 结构化变异）循环独立运行，日志 `~/fuzz-lab/ort_fuzz_loop.log`。

---

## 七、明天计划

- [ ] 看 `~/fuzz-lab/ort_fuzz_loop.log` 一夜结果 + 有无 crash
- [ ] 把"ORT 不发表 CVE + fuzz 已打通"整理给导师
- [ ] 审计结果汇报给导师
- [ ] crash 分析工具预热（c++filt + gdb）
