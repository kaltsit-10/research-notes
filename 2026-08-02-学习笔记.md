# 2026-08-02 学习笔记

## 核心收获：两条线都在推进

### 线A：ONNX Runtime fuzz（自动化进行中）
- 语料库自动增长到 **1602 个**（昨天只有 1 个种子）
- 代码覆盖率从 `cov:6` 飙升到 `cov:153`——fuzzer 自己探索更深的代码
- 新加了 3 个结构不同的 ONNX 种子（CNN、MLP、LSTM）

### 线B：FGSM 对抗攻击（今天新开，已完成）

**对真实照片的完整攻击实验**：
- 实验图片：我的玩偶照片 `feibijiubi.jpg`（1440×1920）
- ResNet 原始预测：**咖啡杯** (23.6%置信度)

| epsilon | 噪声强度 | 结果 |
|---------|---------|------|
| 0.001 | 0.001（完全不可见） | 🎉 类别#643 |
| 0.005 | 0.005（完全不可见） | 🎉 类别#643 |
| 0.01 | 0.010（完全不可见） | 🎉 类别#643 |
| 0.02 | 0.020（几乎不可见） | 🎉 类别#796 |
| 0.03 | 0.030（几乎不可见） | 🎉 类别#796 |
| 0.05 | 0.050（勉强可见） | 🎉 类别#796 |
| 0.10 | 0.100（勉强可见） | 🎉 类别#796 |

**7/7 全部攻击成功！**
对比图已保存：`fgsm_feibijiubi.png`（见桌面科研文件夹）

---

## FGSM 是怎么成功的？——梯度驱动的系统性误导

### 四步机制

```
[1] 模型前向传播 → 输出各类别概率 → 选最高（咖啡杯, 23.6%）

[2] 计算 loss = CrossEntropy(预测, "咖啡杯")
    我们希望 loss 增大 → 模型"困惑"

[3] loss.backward() → 计算每个像素的梯度
    梯度 = 这个像素"往哪个方向改"能让模型最困惑
    · 正梯度 → 增大这个像素 → loss↑ → 模型困惑
    · 负梯度 → 减小这个像素 → loss↑ → 模型困惑
    结果：几乎每个像素都有非零梯度！

[4] x_adv = 原图 + epsilon × sign(梯度)
    每个像素朝"让模型困惑的方向"推 epsilon
    224×224×3 = 150,528 个像素，每个推一点点
    → 累积效应 → 特征提取器被系统性误导
    → 模型认错
```

### 为什么这么小的噪声能成功？

```
标准化后的像素值范围大约是 [-2.5, 2.5]
epsilon=0.01 → 每个像素只改了 0.01/2.5 ≈ 0.4%
但关键在于：每个像素都改了 — 150,528 个像素
每个推一点点 → 累积 → 特征提取器被系统性误导
就像：每个汉字改一撇 → 整篇文章的意思完全变了
```

### 玩偶为什么被认成咖啡杯？

ResNet 只在 ImageNet 的 1000 个类别上训练——没有"玩偶"、"手办"这些类。看到不认识的东西只能硬猜最接近的。这不影响攻击结论——不管原预测对不对，FGSM 都能让输出改变。

---

## FGSM 不只是"异常输入"——对比昨天的理解

| | 传统 fuzz | FGSM 对抗攻击 |
|---|---|---|
| 输入来源 | 随机变异种子 | 梯度引导的精确扰动 |
| 目标 | 找内存崩溃 | 让模型逻辑错误（分类错） |
| 可见性 | 输入往往明显畸形 | 输入肉眼和原图一模一样 |
| 检测难度 | ASAN 通常能抓到 | 没有崩溃，只有错误输出 |
| 我的工具 | libfuzzer + ASAN | PyTorch + backward() |

**两者互补**：fuzz 打底层内存安全，FGSM 打上层语义安全。

---

## Fuzz 的形式不止异常输入——更新认知

| 类型 | 做什么 | 我用过吗 |
|------|--------|---------|
| **变异型** | 拿种子，随机改字节 | ✅ `./fuzz_onnx corpus/` |
| **覆盖率引导** | 用代码覆盖率决定保留哪些输入 | ✅ libfuzzer 自动做 |
| **生成型** | 按协议/格式语法生成合法结构 | ❌ |
| **符号执行** | 数学求解"能走到某行的精确输入" | ❌ |
| **差分fuzz** | 同样输入喂给两个相似程序，看输出是否一致 | ❌ |
| **梯度引导（对抗攻击）** | 用梯度反传找"让模型犯错"的扰动 | ✅ 今天 FGSM |

"输入"也不只是文件——还可以是网络包、API调用序列、环境变量、甚至是真实世界的物理信号（声波打IMU、激光打相机）。

**除了 fuzz，挖漏洞还有**：静态分析（扫源码）、符号执行（数学求解精确输入）、人工代码审计（人找逻辑漏洞）、补丁对比（看别人修了什么去挖类似的）。

---

## 这和具身AI的关系

机器人用视觉模型看世界。FGSM 能欺骗图像分类器，同理：
- 自动驾驶把"停止标志"认成"限速60"
- YOLO 把"行人"认成"背景"
- VLA 模型在错误感知下生成危险动作
- 更大的攻击链：**相机画面 + 对抗噪声 → 感知错误 → 规划错误 → 物理危险**

---

## 今天的数字

```
fuzz:   1602 语料, 153 代码块覆盖, 0 crash（正常，继续跑）
FGSM:   7/7 攻击成功（epsilon 0.001~0.10, 真实照片）
```

---

## 明天计划

- [ ] 让 fuzz 继续后台跑
- [ ] 读 CHAI 论文摘要——理解"视觉提示注入"和 FGSM 的区别
- [ ] 学习 crash 分析（`c++filt` + `gdb`），为 fuzz 出 crash 做准备
