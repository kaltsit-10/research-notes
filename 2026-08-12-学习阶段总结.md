# 2026-08-12 学习阶段总结（07-26 → 08-12）

> 阶段：从"库层 fuzz 抽奖"到"国产引擎定向挖洞 + 私密上报"的完整转向。
> 覆盖 07-26 至 08-12，共 5 个里程碑，2 个已上报漏洞 + 1 个未公开新漏洞，方法论从"撞运气"升级为"顺着 CVE 修复找兄弟漏洞"。

---

## 核心收获（本阶段三件事）

1. **两个可上报漏洞落地**：MindSpore `load_checkpoint` 堆溢出（CWE-787，未公开，已上报）＋ NCNN text 层头解析（CWE-125/787，未公开，已上报），均经对抗验证确认为"独立未上报"
2. **方法论成型（比漏洞本身值钱）**：补丁 diff 挖法 → 变体矩阵审计 → 对抗验证评审 → CNVD 原创性核验，四次自我纠错让结论越来越可靠
3. **工具链从"碰运气"到"可复现"**：WSL2 + fuzz 循环 + 复现脚本矩阵 + 证据 SHA-256 归档 + Claude Code Skill 工作流

---

## 一、阶段时间线（5 个里程碑）

| 日期 | 关键事件 | 产出 |
|------|---------|------|
| 07-26~08-02 | 搭 WSL2 + fuzz 工具链；FGSM 对抗样本 7/7 成功；读完三篇综述 | `fgsm_feibijiubi.png`、三篇综述总结 |
| 08-04 | **决策 1**：停掉 ORT 库层 fuzz"抽奖"（protobuf 已被 Google fuzz 烂、cov 饱和、crash 恰好在库层）；转 ORT 本体 | `研究总结与转向决策-2026-08-04.md` |
| 08-05~06 | **决策 2**：ORT 不发表 CVE（MSRC VULN-####）→ 按导师方向转**国产具身智能组件**；调研组件总表 | `审计报告-external_data`、`调研报告-组件总表.md`、`给导师的进展汇报` |
| 08-07 | **成果爆发日**：本地复现 5 个漏洞/变体，挖出 MindSpore 堆溢出新漏洞，补丁 diff 挖法 + 变体矩阵实战 | `复现实验/漏洞归档总览-2026-08-07.md`、588 case 矩阵 |
| 08-08~10 | 私密上报（MindSpore 邮件 + PGP / NCNN GHSA+TSRC）；**事后评估与纠偏**；证据 SHA-256 归档 | `事后评估与归档-2026-08-10/`、`证据文件-SHA256SUMS.txt` |
| 08-11~12 | 微信远程工作台（cc-connect）部署；GitHub Skill 深搜 + 安装 8 个；本总结 | `C:\Users\asus\.claude\skills\` |

---

## 二、成果清单（最终状态，截至 08-12）

| # | 组件 | 漏洞 | CWE | 公开状态 | 上报价值/渠道 |
|---|------|------|-----|---------|--------------|
| 1 | MindSpore | `load_checkpoint` DoS（dims 内存放大 12.5 亿倍，32B→40GB） | CWE-789 | **已公开**（HF PoC），2.8/2.10 均未修 | 低，仅促修复；华为 PSIRT |
| 2 | MindSpore | `load_checkpoint` **堆溢出**（CopyFromBuffer 忽略 dst_size） | CWE-787 | **未公开**，2.8/2.10/master 均未修 | 🥈 中高，已上报 |
| 3 | NCNN | 负 ID 越界写 | CWE-787 | 已修（CVE-2026-50144） | 无，仅对照 |
| 4 | NCNN | 96B NULL 解引用（bin 数组 len 未校验） | CWE-476 | **已公开**（HF PoC），master 未修 | 低，仅促修复 |
| 5 | NCNN | **text 层头解析**（top_count 越行读取 → 越界读/SIGABRT/巨大分配） | CWE-125/787/755/789 | **未公开**，master 未修 | 🥇 高，已上报（GHSA/TSRC） |

**上报优先级排序（08-07 对抗验证结论）**：NCNN text 层头（HIGH）> MindSpore 堆溢出（MEDIUM）> 两个已公开 DoS（促修复）。

---

## 三、五个关键决策与转折（含教训）

### 1. 停掉"库层抽奖"（08-04）
> 一直跑自动化 fuzz 是低概率碰运气——**fuzz 本身没错，是目标选错了**。
- protobuf 是 Google 天天 fuzz 的库；cov 2588 饱和；第一个 crash 恰好在库层（不是 ORT 本体）
- 教训：先做**期望值分析**再决定跑不跑，别让算力掩盖方向错误

### 2. ORT 不发表 CVE → 转国产引擎（08-06）
> ORT 漏洞走 MSRC 私密追踪（VULN-143188 先例），无 CVE 出口 → 换有 CVE 流程的国产引擎
- 证据：onnxruntime issue #23258 → VULN-143188，无 CVE
- 教训：**成果出口决定目标选择**——"能找到 bug"和"能拿编号"是两回事

### 3. 蓝海 → 被认可（08-06，自我纠错）
> 从"搜索没找到近期研究"推出"5 年无人审 MindSpore" → **被用户质疑推翻**
- 真相：MindSpore 中国新增市场 30%、官方安全团队、近期 CVE（2023/2025）、Valhalla 审计刚发、DoS PoC 本身就是别人发现的
- 方法论：**absence of evidence ≠ evidence of absence**。评估竞争看"官方安全团队/近期 CVE 活跃度/第三方审计/公开 PoC"，不是看搜索结果空不空

### 4. DoS 升级为堆溢出（08-07，最大转折）
> 审计"已知漏洞的兄弟方向"比从零挖快——已知 DoS = "dims 大内容小"，反方向"dims 小内容大"就是堆溢出。
- 同一个校验缺失的两个侧面：`memcpy_s(dst, remain_size, src, remain_size)` 把 src 长度当 dst 缓冲大小 → **安全函数被绕过**
- `memcpy_s` ≠ 安全：检查安全函数的**参数来源**是否可信
- **崩溃点 ≠ 漏洞点**：gdb 栈显示崩溃在 `Py_FinalizeEx`（下游），真正的写点在 `CopyFromBuffer`——需要边界阈值实验（≥24B 越界）定位

### 5. CNVD 原创性核验（08-07/08-10，两次过度自信纠正）
> "无编号" ≠ "上报能拿编号"——CNVD 三级审核含**原创性核验**（对互联网公开漏洞查重）。
- DoS 已公开（HF PoC）→ 原创性大概率不过 → 只能当"已知漏洞"收录
- 只有未公开的堆溢出/层头漏洞是合格新漏洞候选
- 正确路径：**先厂商确认未修 → 再 CVE/CNVD**，勿先报 CNVD、勿直接公开

---

## 四、方法论沉淀（本阶段最大资产）

```
1. 补丁 diff 挖法：拿到 CVE 修复 commit → diff → 找"只堵一个口子"的单行检查
   → 顺着修复找兄弟分支（text vs bin / 数组 vs 字符串）→ 比从零挖快 10 倍
2. 变体矩阵审计：dims 边界 × 内容长度 × dtype 全组合（588 case）
   → 自动暴露"只拦特定值"的校验缺陷（parameter.py 只查 -1，绕过其他负值）
3. 对抗验证评审：指定唱反调的评审视角 → 抓住"可行性评级过度乐观"的硬伤
   （RCE 自举循环依赖：fastbin 投毒需堆基址，唯一泄漏源又需先有受控重叠）
4. 诚实边界：ASan 区分 READ vs WRITE → ncnn 主 CWE 从 787 修正为 125；
   I:H 评级必须绑定直接证据（无越界写证据就不能写 I:H）
5. 证据归档：非破坏式索引 + SHA-256 清单，修复前 PoC 不公开
```

**阶段级洞察**：模型文件反序列化（.ckpt/.param/.parambin/.gguf/.pdparams）→ 整数溢出/堆溢出/RCE = **AI 基础设施头号漏洞类**；国产引擎同类代码几乎没被系统性 fuzz（4 个国产组件均不在 OSS-Fuzz，Google 官方 introspector 确认）。

---

## 五、Skill 工作流（08-12 新增）

### 已安装（`C:\Users\asus\.claude\skills\`，8 个）
| Skill | 用途 |
|-------|------|
| `libfuzzer` / `aflpp` / `address-sanitizer` / `coverage-analysis` | fuzz 目标、消毒器、覆盖率 |
| `harness-writing` / `fuzzing-dictionary` | harness 编写、模型格式字典 |
| `security-review` | 源码安全审查 |
| `oss-forensics` | 上游修复状态/开源披露调查 |

### 标注规则（本总结起生效）
每次调用 Skill 都在回复中显式标注：
```
【Skill】使用了 `oss-forensics`：核查 ncnn/MindSpore 上游是否已修复上报漏洞
【Skill】使用了 `harness-writing` + `fuzzing-dictionary`：设计 ncnn paramdict fuzz 目标
```

### 关于"为什么之前没看到 Skill 标注"的如实说明
- Skill 的**加载**（能否被识别、指令是否进入上下文）和**显式标注**（回复里写"正在使用 X skill"）是两件事。能否被识别取决于 harness 与模型配合；是否在回复里打印标注取决于调用方是否有此约定
- 本会话（当前驱动模型）实际上**调用过 skill-creator**，且 8 个新装 skill 已被识别——说明 Skill 机制在本环境是可用的
- 之前没看到标注，主要因为**前几天的场景确实很少触发 Skill**（复现/审计多数是手工流程 + 受限网络），不是机制坏了
- **从今天起我承诺：每次调用 Skill 都在输出里标注名称与用途**，让你能核对

---

## 六、诚实审计与纠偏记录（08-10 归档要点）

| 项 | 原判断 | 纠偏后 | 依据 |
|----|--------|--------|------|
| ncnn 主 CWE | CWE-787 越界写 | **CWE-125 越界读** | ASan 原始证据 `READ of size 8` @ net.cpp:1464 |
| ncnn 影响评级 | `I:H` | 保守（约 CVSS 5.5） | 无越界写证据，`I:H` 必须绑定直接证据 |
| MindSpore `I:H` | 偏积极 | 保守化 | 同一长度校验缺失与已知 DoS 同源，RCE 未证实 |
| CNVD-2026-23196 | 曾拟引用 | **不引用** | 疑似串号捏造（内容农场唯一来源，同号 CVE 实为 Linux HID） |
| MindSpore DoS "已报厂商" | 未核实 | 待核实 | 仅 HF PoC 声称 |

**上报身份红线（全程遵守）**：署名 `kaltsit-10`，个人独立研究者；不使用真实姓名/导师课题组字眼/虚构组织；QQ 邮箱 `3270694207@qq.com`；PGP 加密邮件，PoC 修复前不公开。

---

## 七、当前状态与下一步

### 等待中
- MindSpore / NCNN 上报回信（MindSpore 安全 SIG 外部邮件有 7 天答复时效，已订阅，避免重复发送第三封）

### 下一步候选（按价值排序）
1. **用 Skill 复核上游修复状态**：ncnn `master` 是否已碰 `net.cpp` 层头/`paramdict`；MindSpore master 是否已动 `CopyFromBuffer`
2. **ncnn paramdict fuzz 目标**：把手工 PoC 升级为结构感知 fuzz（harness + 字典 + ASan），系统性补审 len 校验/字符串分支
3. **MindIR 加载链独立审计**：`load_mindir` C++ 解析链与 CopyFromBuffer 家族独立（源码可得时优先）
4. **新组件攻击面**：MNN（阿里，C++ 几乎无审计）、MegEngine/OneFlow（旷视/一流，无公开）、国产世界模型/VLA（蓝海，需 GPU）；PaddlePaddle 仅对照
5. **等待厂商回复后的流程**：确认修复版本、公开时间表、CVE 申请

---

## 附：本阶段关键文件索引

| 文件 | 内容 |
|------|------|
| `笔记/2026-08-0X-学习笔记.md` | 每日笔记（07-26 至 08-07） |
| `复现实验/漏洞归档总览-2026-08-07.md` | 5 个漏洞完整复现流程 + 上报评估 |
| `复现实验/事后评估与归档-2026-08-10/` | 事后评估、纠偏建议、SHA-256 清单 |
| `调研报告-组件总表.md` | 国产组件调研总表 + 交叉验证状态 |
| `研究总结与转向决策-2026-08-04.md` | 第一阶段转向决策 |
| `复现实验/mindspore/漏洞分析-MindSpore-堆溢出-2026-08-07.md` | 堆溢出完整根因 + 矩阵 + RCE 评审 |
| `复现实验/ncnn/漏洞分析-NCNN-2026-08-07.md` | ncnn bin/text 参数解析分析 |
| `C:\Users\asus\.claude\skills\` | 8 个已装 Skill |
